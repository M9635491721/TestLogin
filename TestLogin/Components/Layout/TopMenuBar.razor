<nav class="navbar navbar-expand-lg navbar-dark sticky-top portfolio-nav">
    <div class="container">
        <a class="navbar-brand fw-bold" href="#">Kamruddin</a>

        <button class="navbar-toggler" type="button" @onclick="ToggleNav" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse @(isNavCollapsed ? "" : "show")">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                <li class="nav-item">
                    <NavLink class="nav-link" href="" Match="NavLinkMatch.All">Home</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="about">About</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="skills">Skills</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="contacts">Contacts</NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    private bool isNavCollapsed = true;
    private void ToggleNav() => isNavCollapsed = !isNavCollapsed;
}